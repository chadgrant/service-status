// Code generated by goa v3.0.9, DO NOT EDIT.
//
// servicestatus gRPC client CLI support package
//
// Command:
// $ goa gen github.com/chadgrant/servicestatus/design

package cli

import (
	"flag"
	"fmt"
	"os"

	environmentc "github.com/chadgrant/servicestatus/gen/grpc/environment/client"
	goa "goa.design/goa/v3/pkg"
	grpc "google.golang.org/grpc"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//    command (subcommand1|subcommand2|...)
//
func UsageCommands() string {
	return `environment (list|add|update)
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` environment list` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(cc *grpc.ClientConn, opts ...grpc.CallOption) (goa.Endpoint, interface{}, error) {
	var (
		environmentFlags = flag.NewFlagSet("environment", flag.ContinueOnError)

		environmentListFlags = flag.NewFlagSet("list", flag.ExitOnError)

		environmentAddFlags       = flag.NewFlagSet("add", flag.ExitOnError)
		environmentAddMessageFlag = environmentAddFlags.String("message", "", "")

		environmentUpdateFlags       = flag.NewFlagSet("update", flag.ExitOnError)
		environmentUpdateMessageFlag = environmentUpdateFlags.String("message", "", "")
	)
	environmentFlags.Usage = environmentUsage
	environmentListFlags.Usage = environmentListUsage
	environmentAddFlags.Usage = environmentAddUsage
	environmentUpdateFlags.Usage = environmentUpdateUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "environment":
			svcf = environmentFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "environment":
			switch epn {
			case "list":
				epf = environmentListFlags

			case "add":
				epf = environmentAddFlags

			case "update":
				epf = environmentUpdateFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     interface{}
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "environment":
			c := environmentc.NewClient(cc, opts...)
			switch epn {
			case "list":
				endpoint = c.List()
				data = nil
			case "add":
				endpoint = c.Add()
				data, err = environmentc.BuildAddPayload(*environmentAddMessageFlag)
			case "update":
				endpoint = c.Update()
				data, err = environmentc.BuildUpdatePayload(*environmentUpdateMessageFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// environmentUsage displays the usage of the environment command and its
// subcommands.
func environmentUsage() {
	fmt.Fprintf(os.Stderr, `The environment service performs operations on environments
Usage:
    %s [globalflags] environment COMMAND [flags]

COMMAND:
    list: List all environments
    add: adds a new environment and returns the url of where to retrieve it
    update: update an existing environment

Additional help:
    %s environment COMMAND --help
`, os.Args[0], os.Args[0])
}
func environmentListUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] environment list

List all environments

Example:
    `+os.Args[0]+` environment list
`, os.Args[0])
}

func environmentAddUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] environment add -message JSON

adds a new environment and returns the url of where to retrieve it
    -message JSON: 

Example:
    `+os.Args[0]+` environment add --message '{
      "active": true,
      "friendly": "development",
      "key": "Pariatur ut.",
      "name": "Production",
      "sort": 0
   }'
`, os.Args[0])
}

func environmentUpdateUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] environment update -message JSON

update an existing environment
    -message JSON: 

Example:
    `+os.Args[0]+` environment update --message '{
      "active": true,
      "friendly": "development",
      "key": "In inventore ut.",
      "name": "Production",
      "sort": 0
   }'
`, os.Args[0])
}
